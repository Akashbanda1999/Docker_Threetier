services:
    medi:
      build: .
      ports:
        - '80:80'
      container_name: Medicontainer
      volumes:
        - myapp2:/var/www/html/
      network_mode: 
          bridge
volumes:
   myapp2: